% Appendix A - Reproducibility
% Code-level and engineering detail kept out of the body lives here.

\chapter{Reproducibility}
\label{app:reproducibility}

This appendix records the engineering detail needed to reproduce the system and its results, kept out of
the main text so that the body reads as a dissertation rather than a software specification.

\section{Environment}
\label{app:repro_env}

The system is implemented in Python~3.12, chosen for the maturity of its packages for the
machine-learning stack. Dependencies are pinned and frozen in a lock file so the environment can be
recreated across machines. The numerical and data handling rely on established open-source libraries;
sentence embeddings use the Sentence-BERT framework on a CPU build of the underlying deep-learning
library, installed from a dedicated index to avoid the much larger GPU download; figures are produced
with a standard plotting library. Table~\ref{tab:repro_versions} lists the pinned versions of the core
dependencies; the complete set is frozen in the repository's lock file.

\begin{table}[ht]
\caption{Pinned versions of the core dependencies (Python~3.12).}
\label{tab:repro_versions}
\centering
\begin{tabular}{l l}
\toprule
\tabhead{Library} & \tabhead{Version} \\
\midrule
numpy & 2.1.3 \\
pandas & 2.2.3 \\
matplotlib & 3.11.0 \\
scikit-learn & 1.9.0 \\
yfinance & 1.4.1 \\
sentence-transformers & 5.6.0 \\
transformers & 5.12.1 \\
torch (CPU build) & 2.12.1 \\
pytest & 8.3.4 \\
ruff & 0.8.6 \\
\bottomrule
\end{tabular}
\end{table}

\section{Repository Organisation}
\label{app:repro_repo}

The code is organised as one package per component, mirroring the architecture of
Figure~\ref{fig:architecture}: market data, news retrieval, anomaly detection, the correlation engine
(similarity and event study), the historical knowledge base, the explanation engine, and alert delivery,
with a small orchestration layer wiring them into the two end-to-end flows. Pure logic is separated from
input/output, and heavy or networked dependencies are loaded lazily, so the numerical core is unit-tested
without a network connection or the model stack. Secrets are read only from a local, ignored environment
file and are never written to versioned files.

\section{The Complete Pipeline on One Page}
\label{app:repro_pipeline}

The body of the dissertation deliberately presents the system through several small, simple diagrams
(architecture, data model, per-trigger flows, the life of one alert). Figure~\ref{fig:master_pipeline}
is the opposite view, kept in the appendix precisely because it is dense: the \emph{complete} deployed
pipeline on a single landscape page, with every gate and its production configuration value annotated.
Each value is a disclosed deployment parameter (the evaluation of Chapter~\ref{chap:casestudies} freezes
its own, stricter settings, as noted on the figure).

\begin{sidewaysfigure}[p]
\centering
\begin{tikzpicture}[
  font=\scriptsize, >={Stealth[length=2mm]},
  box/.style={draw, rounded corners, align=center, minimum height=0.78cm, inner sep=2.5pt},
  db/.style={box, fill=black!8},
  pr/.style={box, fill=black!3},
  gate/.style={box, dashed, fill=yellow!8},
  dlv/.style={box, fill=black!14},
  note/.style={font=\tiny, text=black!55, align=center},
  lane/.style={font=\itshape\scriptsize, text=black!55, anchor=west},
]
  % ── lane: offline KB (top) ─────────────────────────────────────────────
  \node[lane] at (-0.4,12.9) {offline build};
  \node[db]  (o1) at (1.6,12.2)  {FNSPID 2018--23\\ news $+$ prices};
  \node[pr]  (o2) at (5.0,12.2)  {align to event day;\\ measure $+1/+3/+5$\,d};
  \node[pr]  (o3) at (8.4,12.2)  {embed\\ (MiniLM, $384$-d)};
  \node[db]  (kb) at (11.6,12.2) {historical KB};
  \draw[->] (o1)--(o2); \draw[->] (o2)--(o3); \draw[->] (o3)--(kb);

  % ── lane: living KB ────────────────────────────────────────────────────
  \node[lane] at (-0.4,10.6) {living KB};
  \node[pr]  (l1) at (5.0,10.0)  {pending case\\ (embedded at capture)};
  \node[gate](l2) at (8.4,10.0)  {mature: age $\ge 8$\,d\\ and $+5$\,d bar observable};
  \node[db]  (l3) at (11.6,10.0) {living KB};
  \draw[->] (l1)--(l2); \draw[->] (l2)--(l3);

  % ── lane: news trigger ─────────────────────────────────────────────────
  \node[lane] at (-0.4,8.4) {news trigger};
  \node[db]  (n1) at (1.6,7.7)  {company news\\ (Finnhub, 7-day scan)};
  \node[gate](n2) at (5.0,7.7)  {relevance: names the\\ company; no boilerplate};
  \node[gate](n3) at (8.4,7.7)  {fresh\\ $\le 2$ days};
  \node[pr]  (n4) at (11.6,7.7) {retrieve top-3:\\ cosine $\times\,0.5^{\text{age}/120\text{d}}$};
  \node[gate](n5) at (14.8,7.7) {floor: best\\ cosine $\ge 0.45$};
  \node[gate](n6) at (17.9,7.7) {triage LR $+$ Platt\\ $p \ge 0.5$};
  \draw[->] (n1)--(n2); \draw[->] (n2)--(n3); \draw[->] (n3)--(n4);
  \draw[->] (n4)--(n5); \draw[->] (n5)--(n6);
  \draw[->] (n2) -- node[right, note]{capture} (l1);
  \draw[->] (kb) -- (n4);
  \draw[->] (l3) -- (n4);

  % ── lane: market trigger (close) ───────────────────────────────────────
  \node[lane] at (-0.4,5.9) {market trigger (close)};
  \node[db]  (m1) at (1.6,5.2)  {daily closes:\\ yfinance $\to$ Tiingo $\to$\\ Polygon $\to$ Stooq $\to$ AV};
  \node[gate](m2) at (5.0,5.2)  {fresh bar\\ (today's session)};
  \node[gate](m3) at (8.4,5.2)  {$|z| > 1.5$ vs 20-d norm\\ \tiny (eval.\ freezes $3.0$)};
  \node[pr]  (m4) at (11.6,5.2) {severity: notable $\ge$1.5\\ strong $\ge$2 · extreme $\ge$3};
  \node[pr]  (m5) at (14.8,5.2) {sector check\\ (same-day peers)};
  \node[pr]  (m6) at (17.9,5.2) {cross-investigate:\\ explaining news (48\,h)};
  \draw[->] (m1)--(m2); \draw[->] (m2)--(m3); \draw[->] (m3)--(m4);
  \draw[->] (m4)--(m5); \draw[->] (m5)--(m6);

  % ── lane: market trigger (intraday) ────────────────────────────────────
  \node[lane] at (-0.4,3.4) {market trigger (intraday)};
  \node[gate](i1) at (1.6,2.7)  {US session?\\ Mon--Fri 13:00--21:30 UTC};
  \node[db]  (i2) at (5.0,2.7)  {live quote\\ (Finnhub, real-time)};
  \node[pr]  (i3) at (8.4,2.7)  {running return vs\\ 20-d norm (complete days)};
  \draw[->] (i1)--(i2); \draw[->] (i2)--(i3);
  \draw[->] (i3) -- (m4);

  % ── outputs (right) ────────────────────────────────────────────────────
  \node[gate](dd) at (20.9,6.5) {dedup $+$ caps:\\ 1 market/ticker/day;\\ $\le 2$ news/ticker/day};
  \node[pr]  (ex) at (20.9,4.4) {explanation engine\\ \tiny (facts $+$ method $+$\\ \tiny no-prediction clause)};
  \node[dlv] (tg) at (20.9,2.6) {Telegram channel\\ $+$ bot subscribers};
  \node[dlv] (hi) at (17.9,1.0) {shared history\\ (data branch)};
  \node[dlv] (da) at (14.8,1.0) {public dashboard\\ (reads, never recomputes)};
  \draw[->] (n6.east) -- (dd.west);
  \draw[->] (m6.east) -- (dd.west);
  \draw[->] (dd)--(ex); \draw[->] (ex)--(tg);
  \draw[->] (tg.south) |- (hi.east);
  \draw[->] (hi)--(da);

  % ── daily summary ──────────────────────────────────────────────────────
  \node[pr] (su) at (11.6,3.9) {daily summary\\ (first run $\ge$ 21:00 UTC)};
  \draw[->] (m3.south) |- (su.west);
  \draw[->] (su.east) -- (ex.west);

  % legenda
  \node[gate, minimum height=0.5cm] (lg1) at (1.6,0.6) {\tiny gate (drops items)};
  \node[pr, minimum height=0.5cm, right=3mm of lg1] (lg2) {\tiny computation};
  \node[db, minimum height=0.5cm, right=3mm of lg2] (lg3) {\tiny data source};
  \node[dlv, minimum height=0.5cm, right=3mm of lg3] (lg4) {\tiny delivery};
\end{tikzpicture}
\caption[The complete deployed pipeline, with every gate annotated]{The complete deployed pipeline on
one page: the offline and living knowledge bases, the news trigger with its five quality gates, the
close-based and intraday market triggers with the multi-source price fallback chain, and the shared
outputs. Dashed boxes are gates that drop items; the annotated values (thresholds, caps, decay
half-life) are the disclosed production configuration, distinct from the frozen evaluation settings of
Chapter~\ref{chap:casestudies}. The body chapters present the same system as several simpler views;
this figure exists so that no stage is left implicit.}
\label{fig:master_pipeline}
\end{sidewaysfigure}

\section{Tests and Reproducible Results}
\label{app:repro_tests}

The system is covered by an automated test suite spanning the anomaly detector, the event study,
similarity, the knowledge base, the news parser, the explanation engine and the evaluation modules,
together with an offline end-to-end smoke test of the news trigger. Tests that require network access or
heavy models are marked and excluded from the default run so that it is fast, deterministic and offline.
All experimental results in Chapter~\ref{chap:casestudies} are regenerated by scripts with a fixed random
seed, and the result figures are written directly into the thesis figure directory, closing the loop
between code and document. The four case studies are reproduced by four commands, each writing its tables
and figures into the evaluation and figure directories:

\begin{itemize}
  \item Precedent retrieval (precision@$k$, Case Study~2): \texttt{python scripts/evaluate.py}
  \item Per-sector retrieval breakdown (Case Study~2):\\ \texttt{python scripts/evaluate\_per\_sector.py}
  \item Anomaly firing-rate and window ablation (Case Study~1):\\ \texttt{python scripts/evaluate\_anomaly.py}
  \item Materiality triage: dataset build and training (Case Study~4):\\
        \texttt{python scripts/build\_dataset.py} followed by \texttt{python scripts/train\_triage.py}
  \item Extended detector and volatility comparison (Case Study~1):\\
        \texttt{python scripts/evaluate\_anomaly\_ext.py}
  \item Real embedding-space projection (Case Study~2):\\
        \texttt{python scripts/figures/fig\_embedding\_projection.py}
  \item Worked triage decomposition and production funnel (Chapters~\ref{chap:methods}
        and~\ref{chap:investigator}):\\
        \texttt{python scripts/figures/fig\_triage\_worked.py} and
        \texttt{python scripts/figures/fig\_alert\_funnel.py}
\end{itemize}

\noindent The data-fetch step that precedes them, and the full build of the historical knowledge base, are
documented in the repository's operator guide.

\section{Proof of Work: Every Number Traced to Its Source}
\label{app:repro_proof}

No result in this dissertation is typed by hand. Each script writes its numbers and figures directly
into a frozen file under \texttt{docs/evaluation/}, and the thesis quotes those files, so the loop
between code and document is closed and auditable. Table~\ref{tab:proof_of_work} lists, for every
headline result, the value as reported, and the frozen file that a single command from
Section~\ref{app:repro_tests} regenerates. An examiner can reproduce any one number in isolation.

\begin{table}[ht]
\caption{Each reported result, its value, and the frozen file one command regenerates.}
\label{tab:proof_of_work}
\centering
\small
\begin{tabular}{p{6.1cm} l l}
\toprule
\tabhead{Result} & \tabhead{Value} & \tabhead{Frozen file} \\
\midrule
Precedent retrieval, precision@5 (MiniLM / MPNet / lexical) & 0.514 / 0.538 / 0.346 & evaluation\_results.md \\
Anomaly detection, $F_1$ (z-score vs fixed threshold) & 0.516 vs 0.218 & evaluation\_anomaly.md \\
Statistical rule vs learned detectors, $F_1$ (z-score / IF / LOF) & 0.530 / 0.269 / 0.280 & evaluation\_anomaly\_ext.md \\
EWMA volatility (validated future), $F_1$ vs rolling & 0.664 vs 0.516 & evaluation\_anomaly\_ext.md \\
Materiality triage, PR-AUC (volatility / context / full) & 0.542 / 0.538 / 0.496 & evaluation\_triage.md \\
Triage precision@5 per day vs alert-always & 0.632 vs 0.163 & evaluation\_triage.md \\
Extended feature ablation, PR-AUC (context $+$ 5 signals) & 0.535 ($\Delta{-}0.002$) & evaluation\_triage\_ext.md \\
Worked triage decision (META, 12 Jul 2026) & $p = 0.539$ (54\%) & triage\_worked\_example.md \\
Production selectivity (headlines : alerts sent) & 22\,:\,1 & alert\_funnel.md \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The system really ran.} Beyond reproducibility on demand, InvestiGator operated live. A
scheduled workflow scanned the watchlist after each US market close and delivered explained alerts to a
public Telegram channel; the shared, versioned alert record accumulated dozens of real alerts, including
the first genuine market anomaly (NVDA, 13~July~2026). The live knowledge base matured its captured
headlines against the real prices that followed, and the post-validation loop labelled real past
decisions with their observed outcome, reporting a kept-alert precision of $0.667$ against a base rate of
$0.455$ --- out-of-sample evidence that the triage mechanism holds. The automated suite of $199$ tests,
offline and deterministic, runs on every push through continuous integration, and the work is recorded
across more than two hundred commits. Regenerable numbers, a live deployment, and a green test suite
together are the dissertation's proof of work.

\section{Data and Attribution}
\label{app:repro_data}

Large data files are never versioned; they are recreated by the data scripts, and only small samples
are committed. The trained triage models are the exception by design: they are small, so the fitted
bundles are versioned in the repository together with JSON metadata (training window, seed, threshold
and metrics), which makes the deployed model itself inspectable and reproducible. The FNSPID dataset \autocite{dong2024fnspid} is used under its CC~BY-SA~4.0 licence, with
attribution as required; the live market-data and news services are used within their free tiers.
