% Capítulo 4 — Methodology
% RASCUNHO (Sessão 7). EN-GB. Base: docs/arquitectura_sistema.md + docs/evaluation_design.md.
% Diagrama de arquitetura em TikZ (reprodutível). Citações verificadas. Rever/editar — §6.6.

\chapter{Methodology}
\label{chap:methodology}

This chapter describes how the system is built. It presents the overall architecture and the two triggers, the
two data layers, the method chosen for each component (with its justification), the evaluation design, and the
rigour guarantees. Throughout, the guiding principle is \emph{defensible simplicity}: where options perform
comparably, the more transparent and explainable one is preferred.

\section{System Overview and Architecture}
\label{sec:met_architecture}

The system is organised as a pipeline driven by two independent triggers: an abrupt market movement and an
incoming news item. Figure~\ref{fig:architecture} shows the components and the flow of data between them. The
live layer supplies real-time prices and news; the historical knowledge base, built from FNSPID, supplies the
precedents; the anomaly detector and the correlation engine produce the evidence; the explanation engine
assembles a traceable account; and the Telegram bot delivers the alert.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[
  box/.style={draw, rounded corners, align=center, minimum height=0.95cm, text width=3.0cm, font=\small},
  >={Stealth[length=2mm]},
  node distance=0.95cm and 1.0cm
]
  \node[box] (live1) {Market data\\ \footnotesize (yfinance / Finnhub)};
  \node[box, right=of live1] (live2) {News fetcher\\ \footnotesize (Finnhub / RSS)};
  \node[box, below=of live1] (anom) {Anomaly detector\\ \footnotesize (\emph{z}-score)};
  \node[box, below=of live2] (corr) {Correlation engine\\ \footnotesize (SBERT, cosine, event-study)};
  \node[box, right=of corr] (kb) {Historical KB\\ \footnotesize (FNSPID)};
  \node[box, below=of corr, xshift=-1.5cm] (expl) {Explanation engine\\ \footnotesize (rules, precedents, SHAP)};
  \node[box, below=of expl] (bot) {Telegram bot\\ \footnotesize (alert)};
  \draw[->] (live1) -- (anom);
  \draw[->] (live2) -- (corr);
  \draw[->] (kb) -- (corr);
  \draw[->, dashed] (anom) -- (corr) node[midway, above, font=\scriptsize] {context};
  \draw[->] (anom) |- (expl);
  \draw[->] (corr) -- (expl);
  \draw[->] (expl) -- (bot);
\end{tikzpicture}
\caption[System architecture and data flow]{System architecture. Solid arrows show the data flow; the dashed
arrow shows the anomaly providing context to the correlation engine. The two triggers (market movement via the
anomaly detector; news via the correlation engine) converge on the explanation engine.}
\label{fig:architecture}
\end{figure}

\section{Data Architecture: Historical vs. Live}
\label{sec:met_data}

The system uses two clearly separated data layers. The \textbf{historical} layer is built offline from FNSPID
\autocite{dong2024fnspid}, which provides financial news already time-aligned with prices; for each historical
news item the system stores its text, date, ticker, a sentence embedding, and the observed post-event price
impact. The \textbf{live} layer provides, in real time, prices (yfinance, with Finnhub as a fallback) and news
(Finnhub and financial RSS feeds). Free news APIs are not used to build history because their free tiers are
typically limited to recent windows and are delayed; the exact ticker subset, time window and preprocessing are
documented in the data card.

\section{Statistical Anomaly Detection}
\label{sec:met_anomaly}

Abrupt movements are detected with a transparent statistical rule. Let $r_t$ be the (log-)return of an asset on
day $t$, and let $\mu_t$ and $\sigma_t$ be the mean and standard deviation of returns over a trailing window of
$w$ days. The day is flagged as anomalous when the absolute $z$-score exceeds a threshold $k$:
\begin{equation}
z_t = \frac{r_t - \mu_t}{\sigma_t}, \qquad \text{anomaly} \iff |z_t| > k .
\label{eq:zscore}
\end{equation}
This statistical family is standard \autocite{chandola2009anomaly} and is chosen for its transparency: the user
can see exactly why an alert fired. More expressive detectors such as Isolation Forest
\autocite{liu2008isolation} were considered but not adopted as the default, because their reduced
interpretability conflicts with the XAI-first goal; the window $w$ and threshold $k$ are documented design
choices, evaluated in Chapter~\ref{chap:evaluation}.

\section{News--Market Correlation Engine}
\label{sec:met_correlation}

This is the core of the work. Each news item is encoded into a sentence embedding using a Sentence-BERT model
\autocite{reimers2019sbert}, chosen because it allows efficient comparison of texts through cosine similarity.
Given a new item, the engine retrieves the $k$ most similar historical news items from the FNSPID-based knowledge
base and reports the impact those precedents had. Impact is measured in the manner of an event study
\autocite{brown1985daily}: the asset's return is computed over fixed post-event windows (e.g. $+1$, $+3$ days).
The similarity metric and the impact windows are explicit design choices and form part of the contribution.
Crucially, impact is measured strictly after the event, so no future information leaks into the features
(Section~\ref{sec:met_rigour}).

\section{Explanation Engine (XAI)}
\label{sec:met_xai}

The explanation engine assembles a traceable account for every alert, combining three transparent ingredients:
(i) the rule or measure that produced the detection (e.g. the $z$-score and threshold), (ii) the retrieved
historical precedents and their observed impact as evidence, and (iii), optionally, feature attribution over the
detector using SHAP \autocite{lundberg2017shap}. This combination follows the explainability principles set out
in the XAI literature \autocite{arrieta2020xai, adadi2018peeking}: the goal is an explanation the user can follow
step by step. Optionally, financial sentiment from FinBERT \autocite{araci2019finbert} may enrich the
explanation; this component is cuttable if it does not add defensible value.

\section{Evaluation Design}
\label{sec:met_evaluation}

The evaluation is defined in advance, to avoid fishing for favourable results, and is reported in
Chapter~\ref{chap:evaluation}. In summary: the anomaly detector is assessed with precision, recall and F1 against
an explicit definition of ``true'' anomalies, with a baseline and an ablation over window and threshold; the
correlation engine is assessed on retrieval quality (precision@$k$ with a relevance rubric, against random and
recency baselines) and on the measured event-study impact; and the explanation engine is assessed for fidelity
to the actual logic and for usefulness, via a small human-judgement rubric. Because the system performs neither
price prediction nor trading, profit-based metrics are explicitly out of scope.

\section{Methodological Rigour}
\label{sec:met_rigour}

Three guarantees apply throughout. First, \textbf{no lookahead}: features at any instant use only information
available at or before that instant, and historical impact uses only post-event windows; this is documented per
experiment. Second, \textbf{reproducibility}: random seeds are fixed, dependencies are pinned, and every
data/results figure is produced by a script in the repository. Third, \textbf{honesty}: only results that can be
reproduced and explained are reported, and no data, metric or citation is fabricated.
