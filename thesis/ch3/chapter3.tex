% Chapter 3 - Methods and Materials
% Methodology, datasets (data card), models/techniques (conceptual), tools, responsible AI, evaluation design.
% No code identifiers in the body (engineering described at design level). EN-GB.

\chapter{Methods and Materials}
\label{chap:methods}

This chapter sets out how the system was built and how it is judged: the overall approach, the datasets
(documented in a reproducible data card), the models behind each component, the responsible-AI
considerations, and the evaluation design, which was fixed before any experiment was run. One idea runs
through all of it: keep things as simple as they can defensibly be, so that where two options perform
about the same, the more transparent one wins. CLARION, the system these methods produce, is described in
Chapter~\ref{chap:clarion}; the experiments that apply them are reported in
Chapter~\ref{chap:casestudies}.

\section{Approach and Methodology}
\label{sec:met_approach}

The work follows an incremental, end-to-end engineering methodology. Rather than building each component
to completion in isolation, a thin slice of the system was first built and proven end to end --- from
data acquisition through to a delivered alert --- so that integration risk was identified and controlled
early. Each component was then developed in its simplest defensible form and made more sophisticated only
where the added complexity was justified by a measurable or clearly-argued benefit. This reflects the
framing of the work as Artificial Intelligence \emph{engineering}: as discussed in
Chapter~\ref{chap:sota}, the individual building blocks already exist in the literature, so the
contribution lies in their disciplined integration, application and critical evaluation, not in the
invention of new algorithms.

Two consequences of this methodology recur throughout the design. First, every component is specified by
its \emph{responsibility} and its \emph{interface} rather than by a particular implementation, which
keeps the system testable and makes principled substitution possible (for example, exchanging one
embedding model for another). Second, the methods chosen are constrained by the project's non-negotiable
requirements: only free and reproducible resources, a focus on the US market, transparency at every step,
and no price prediction or trading. These requirements act as a filter on the design space before any
performance consideration.

\section{Datasets and Data Sources}
\label{sec:met_data}

The system uses two clearly separated data layers, sharing a common schema so that they are directly
comparable: a \textbf{historical} layer, built offline, that supplies the precedents, and a
\textbf{live} layer that supplies real-time prices and news (Figure~\ref{fig:data_layers}). This
separation is deliberate: the historical layer must be rich and stable, whereas the live layer must be
timely and inexpensive.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[font=\small, >={Stealth[length=2.2mm]},
  box/.style={draw, rounded corners, align=center, text width=3.0cm, minimum height=1.0cm},
  src/.style={box, fill=black!8}, proc/.style={box, fill=black!3}]
  \node[src] (fnspid) {\textbf{Historical:} FNSPID\\ \footnotesize news aligned to prices};
  \node[proc, right=1.6cm of fnspid] (kb) {Knowledge base\\ \footnotesize past news $+$ observed impact};
  \node[src, below=1.0cm of fnspid] (live) {\textbf{Live:} free feeds\\ \footnotesize prices $+$ news};
  \node[proc, right=1.6cm of live] (now) {Current input\\ \footnotesize new move or headline};
  \draw[->] (fnspid) -- node[above,font=\scriptsize]{align, embed} (kb);
  \draw[->] (live) -- node[above,font=\scriptsize]{normalise} (now);
  \draw[<->, dashed] (kb) -- node[right,font=\scriptsize,text width=2.7cm]{common schema:\\ directly comparable} (now);
\end{tikzpicture}
\caption[The two data layers]{The two data layers. The historical layer turns the FNSPID corpus into a
knowledge base of past news and its observed price impact; the live layer normalises real-time prices and
news into the same fields, so a current event can be compared directly against the precedents.}
\label{fig:data_layers}
\end{figure}

\subsection{Historical Layer: the FNSPID Corpus}
\label{sec:met_data_fnspid}

The intended primary source for the historical layer is the FNSPID dataset \autocite{dong2024fnspid},
which provides financial news time-aligned with daily prices for thousands of US companies --- precisely
the alignment required to relate news to subsequent price impact without bespoke scraping. The dataset's
news component is large (on the order of tens of gigabytes), which directly motivates the streaming,
filtered ingestion described below. Table~\ref{tab:met_datacard} records the data card: the choices that
make the historical subset reproducible.

\begin{table}[ht]
\caption{Data card for the historical layer (FNSPID).}
\label{tab:met_datacard}
\centering
\begin{tabular}{p{0.30\textwidth} p{0.62\textwidth}}
\toprule
\tabhead{Item} & \tabhead{Value} \\
\midrule
Source & FNSPID \autocite{dong2024fnspid} (financial news time-aligned with prices) \\
Licence & Creative Commons BY-SA 4.0 (attribution required) \\
Fields used & date, ticker, headline (mapped to a common schema) \\
Tickers (15) & AAPL, MSFT, AMZN, GOOGL, NVDA, TSLA, META, JPM, BAC, XOM, CVX, JNJ, PFE, WMT, KO \\
Sectors (5) & Technology, banking, energy, health, consumer staples \\
Time window & 2018--2023 (daily granularity) \\
Prices & Daily closing prices for the same tickers \\
Governance & Large data not versioned (recreated by script); only small samples committed \\
\bottomrule
\end{tabular}
\end{table}

The fifteen large-capitalisation tickers were chosen to span five economic sectors, which is what makes
the cross-sector evaluation of Chapter~\ref{chap:casestudies} meaningful while keeping the corpus
tractable on a modest machine; the six-year window (2018--2023, the extent of the dataset) is recent
enough to be relevant yet long enough to contain precedents. These choices are documented rather than
fixed: a different ticker set or window can be substituted without any change to the methodology.

\subsection{Preprocessing and Event Alignment}
\label{sec:met_data_prep}

Building the historical knowledge base from the corpus involves three steps. First, the news file is read
in a streaming fashion and filtered to the chosen tickers and window, so that only the relevant subset is
materialised rather than the full multi-gigabyte corpus. Second, each retained news item is normalised to
the common schema (date, ticker, headline). Third, each item is aligned to the market and its impact is
measured. The alignment rule is important for honesty: the event day is taken to be the first trading day
\emph{on or after} the news date, and the price impact is measured from that day's closing price onward.
Measuring from the close, rather than the open, avoids crediting the system with a move already embedded
in the opening price, and measuring strictly forward enforces the no-lookahead guarantee of
Section~\ref{sec:met_evaluation}. Items for which no aligned price series is available, or whose forward
window extends beyond the available data, are discarded rather than imputed. Algorithm~\ref{alg:kbbuild}
collects these steps into the build procedure.

\begin{algorithm}[ht]
\caption{Knowledge-base construction with event alignment.}
\label{alg:kbbuild}
\begin{algorithmic}[1]
\Require news items $\{(d_i,c_i,h_i)\}$; daily close prices $P_c$ per ticker; horizons $H=\{1,3,5\}$
\Ensure knowledge base $\mathcal{K}$ of cases $(d,c,h,\mathbf{e},\text{impact})$
\State $\mathcal{K} \gets \emptyset$
\For{each news item $(d_i,c_i,h_i)$}
  \State $e_i \gets$ first trading day $\ge d_i$ in $P_{c_i}$ \Comment{event alignment}
  \If{$e_i$ exists}
    \State $\mathbf{e}_i \gets \mathrm{embed}(h_i)$ \Comment{Sentence-BERT}
    \For{$\tau \in H$}
      \State $\text{impact}_i[\tau] \gets$ return from close($e_i$) to close($e_i{+}\tau$), or \textsc{n/a} if unavailable
    \EndFor
    \State $\mathcal{K} \gets \mathcal{K} \cup \{(d_i,c_i,h_i,\mathbf{e}_i,\text{impact}_i)\}$
  \EndIf
\EndFor
\State \Return $\mathcal{K}$
\end{algorithmic}
\end{algorithm}

\subsection{Live Layer and Evaluation Dataset}
\label{sec:met_data_live}

The live layer provides, in real time, prices from a free market-data service (with a second free
provider as a fallback) and news from a free company-news service and from financial syndication feeds.
Free news services are deliberately \emph{not} used to construct the historical layer, because their free
tiers are typically restricted to a short, recent window and are delayed; they are, however, entirely
adequate as the live trigger and as a source of \emph{real} news for a preliminary evaluation. For the
latter purpose, a set of $3{,}714$ real headlines for the fifteen tickers was collected from the free
company-news service (the most recent months available), and is used for the retrieval study in
Chapter~\ref{chap:casestudies}. This set is genuinely real but recent, which is why the full multi-year
FNSPID build remains the richer source identified as future work.

\subsection{Data Governance and Attribution}
\label{sec:met_data_gov}

Two governance rules apply to both layers. First, large data and models are never versioned: they are
recreated by the ingestion process, and only small illustrative samples are kept under version control.
Second, third-party news text is not republished: the dataset is cited and attributed as required by its
Creative Commons BY-SA 4.0 licence \autocite{dong2024fnspid}, and only minimal headline excerpts appear
in this document.

\section{Models and Techniques}
\label{sec:met_techniques}

\subsection{Statistical Anomaly Detection}
\label{sec:met_anomaly}

Abrupt movements are detected with a transparent statistical rule. Let $r_t$ be the (log-)return of an
asset on day $t$, and let $\mu_t$ and $\sigma_t$ be the mean and standard deviation of returns over a
trailing window of $w$ days that ends strictly before $t$. The day is flagged as anomalous when the
absolute $z$-score exceeds a threshold $k$:
\begin{equation}
z_t = \frac{r_t - \mu_t}{\sigma_t}, \qquad \text{anomaly} \iff |z_t| > k .
\label{eq:zscore}
\end{equation}
This belongs to the statistical family of detectors \autocite{chandola2009anomaly} and is chosen for its
transparency: the investor can be shown exactly why an alert fired, in terms of how many standard
deviations the move represented. As established in Chapter~\ref{chap:sota}, more expressive detectors such
as Isolation Forest \autocite{liu2008isolation} or deep models \autocite{pang2021deep} exist, but their
reduced interpretability is not warranted for a single, low-dimensional return series whose alerts must be
explained to a non-expert. The window $w$ and threshold $k$ are explicit design choices, and their effect
is examined by ablation in Chapter~\ref{chap:casestudies}. Algorithm~\ref{alg:zscore} states the rule
precisely; note that the window for day $t$ uses only days strictly before $t$, so no future information
enters the decision.

\begin{algorithm}[ht]
\caption{Rolling \emph{z}-score anomaly detection (no lookahead).}
\label{alg:zscore}
\begin{algorithmic}[1]
\Require return series $r_1,\dots,r_T$; window $w$; threshold $k$
\Ensure flag $a_t$ for each day, with the quantities behind it
\For{$t \gets w+1$ \textbf{to} $T$}
  \State $W \gets (r_{t-w},\dots,r_{t-1})$ \Comment{strictly earlier days}
  \State $\mu_t \gets \mathrm{mean}(W)$;\quad $\sigma_t \gets \mathrm{std}(W)$
  \State $z_t \gets (r_t - \mu_t)\,/\,\sigma_t$
  \State $a_t \gets \big(|z_t| > k\big)$
\EndFor
\State \Return for each flagged $t$, the tuple $(z_t,\mu_t,\sigma_t,w,k)$ \Comment{exposed to the explanation engine}
\end{algorithmic}
\end{algorithm}

A small worked example shows why this normalisation matters. Suppose two stocks both fall $3.2\%$ today.
The first has been calm lately (recent mean $\mu \approx 0.04\%$, standard deviation $\sigma \approx
0.40\%$); the second is volatile ($\mu \approx 0.10\%$, $\sigma \approx 1.50\%$). The same raw move
produces very different $z$-scores (Table~\ref{tab:zscore_worked}): for the calm stock $z \approx -8.1$,
a clear anomaly, but for the volatile stock $z \approx -2.2$, an ordinary day. A fixed percentage
threshold would flag both identically; the $z$-score does not, which is exactly the behaviour wanted.

\begin{table}[ht]
\caption{Worked example: the same $-3.2\%$ move on a calm and a volatile stock.}
\label{tab:zscore_worked}
\centering
\begin{tabular}{p{0.42\textwidth} c c}
\toprule
\tabhead{Quantity} & \tabhead{Calm stock} & \tabhead{Volatile stock} \\
\midrule
Recent mean $\mu$ & $0.04\%$ & $0.10\%$ \\
Recent standard deviation $\sigma$ & $0.40\%$ & $1.50\%$ \\
Today's return $r$ & $-3.2\%$ & $-3.2\%$ \\
$z$-score $(r-\mu)/\sigma$ & $\mathbf{-8.1}$ & $\mathbf{-2.2}$ \\
Flagged at $k=3$? & yes & no \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Semantic Retrieval and Impact Measurement}
\label{sec:met_correlation}

The news--market correlation engine is the core of the work and combines two techniques. For retrieval,
each news item is encoded into a sentence embedding with a Sentence-BERT model \autocite{reimers2019sbert},
chosen (Section~\ref{sec:sota_finnlp}) because it captures contextual meaning yet produces directly
comparable fixed-length vectors, so that the nearest historical items to a query can be found efficiently
with cosine similarity. For impact, the cumulative return of the asset is measured over fixed post-event
windows ($+1$, $+3$ and $+5$ trading days) in the manner of an event study
\autocite{fama1969adjustment, brown1985daily}. Both the similarity metric and the impact windows are
explicit, documented choices and together constitute the methodological contribution. Impact is measured
strictly after the event, so the evidence characterises an outcome rather than forecasting one.
Algorithm~\ref{alg:retrieval} sets out the retrieval-and-reuse step --- the case-based-reasoning core of
Section~\ref{sec:sota_cbr} --- for an incoming headline.

\begin{algorithm}[ht]
\caption{Precedent retrieval and impact reporting (news trigger).}
\label{alg:retrieval}
\begin{algorithmic}[1]
\Require headline $h$; knowledge base $\mathcal{K}=\{(d_j,c_j,h_j,\mathbf{e}_j,\text{impact}_j)\}$; neighbours $k$; horizon $\tau$
\Ensure top-$k$ precedents with similarity and observed impact, and their mean impact
\State $\mathbf{q} \gets \mathrm{embed}(h)$ \Comment{same Sentence-BERT model as the KB}
\For{each case $j \in \mathcal{K}$}
  \State $s_j \gets \cos(\mathbf{q}, \mathbf{e}_j)$ \Comment{cosine over L2-normalised vectors}
\EndFor
\State $S \gets$ indices of the $k$ largest $s_j$
\State \Return $\{(d_j,c_j,h_j,s_j,\text{impact}_j[\tau]) : j \in S\}$ and $\mathrm{mean}_{j\in S}\,\text{impact}_j[\tau]$
\end{algorithmic}
\end{algorithm}

The impact itself is read off a short window after the event, as Figure~\ref{fig:eventwindow} shows. The
news may arrive on a non-trading day, so the \emph{event day} is the first trading day on or after it;
the return is then accumulated from that day's close to the close $+1$, $+3$ and $+5$ trading days later.
Anchoring on the close, and looking only forward, is what keeps the measure free of lookahead: it records
what happened \emph{after} the event became actionable, never before.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[font=\scriptsize, >={Stealth[length=2.2mm]}]
  \draw[->] (-1.8,0) -- (7.2,0) node[right, font=\tiny]{trading days};
  \foreach \x in {0,1,3,5} {\draw (\x,0.12) -- (\x,-0.12);}
  \node[above, font=\tiny] at (-1.2,0.12) {news $d_i$ (maybe non-trading)};
  \node[below=2pt, align=center, font=\tiny] at (0,-0.12) {event day $e_i$\\ (first trading day $\ge d_i$)};
  \node[below=2pt, font=\tiny] at (1,-0.12) {$+1$};
  \node[below=2pt, font=\tiny] at (3,-0.12) {$+3$};
  \node[below=2pt, font=\tiny] at (5,-0.12) {$+5$};
  \filldraw (0,0) circle (1.4pt);
  \draw[dashed] (-1.2,0.0) -- (-1.2,0.45);
  \draw[->, thick] (0,0.45) -- (1,0.45);
  \draw[->, thick] (0,0.7) -- (3,0.7);
  \draw[->, thick] (0,0.95) -- (5,0.95);
  \node[right, font=\tiny] at (5,0.95) {cumulative return from close($e_i$)};
\end{tikzpicture}
\caption[Event-study impact windows]{How impact is measured. The news date $d_i$ may fall on a
non-trading day; the event day $e_i$ is the first trading day on or after it. The return is then
accumulated forward from the close of $e_i$ over $+1$, $+3$ and $+5$ trading days. Only post-event days
are used, so no future information enters.}
\label{fig:eventwindow}
\end{figure}

Three choices fix \emph{how} that impact is measured, and each is made for transparency as much as for
rigour. The first is the \emph{horizons}: cumulative returns are taken over short windows of $+1$, $+3$
and $+5$ trading days. Short windows are standard practice for daily event studies
\autocite{brown1985daily} and limit the chance that unrelated, market-wide moves come to dominate the
measured reaction --- the longer the window, the more confounding creeps in. The second is the
\emph{baseline against which the move is read}. This work reports the \emph{raw} cumulative return rather
than a market-adjusted \emph{abnormal} return --- the return in excess of a market or expected-return
model, as in a classical cumulative-abnormal-return study. Raw returns are chosen deliberately: they are
directly observable and require no estimated market model that a non-expert could not scrutinise. The
cost, stated plainly, is that a raw return conflates the news reaction with any market-wide move on the
same days, which is precisely the confounding threat examined in Chapter~\ref{chap:casestudies}; market
adjustment is a sound refinement and is recorded as future work. The third choice is \emph{aggregation}:
the headline figure an alert shows is the \emph{mean} impact across the retrieved precedents, a simple and
interpretable summary. Because a mean can mask disagreement, the individual precedents and their impacts
are always displayed alongside it, so the investor sees the spread rather than a single number in
isolation.

A worked example makes the retrieve-and-reuse step concrete. The intuition is conveyed first by
Figure~\ref{fig:embedding_concept}: the embedding places each headline in a vector space where texts on
the same theme fall close together, so a query lands near the precedents that resemble it and far from
unrelated news.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[font=\scriptsize, >={Stealth[length=2mm]}]
  \draw[rounded corners, draw=black!25] (-0.3,-0.3) rectangle (8.8,4.3);
  \node[font=\tiny, text=black!55] at (4.2,4.05) {embedding space (schematic, 2-D for intuition)};
  % AI-themed cluster (filled = retrieved)
  \fill (5.3,2.95) circle (2pt) node[right, font=\tiny] {NVDA guidance (AI chips)};
  \fill (4.95,2.45) circle (2pt) node[right, font=\tiny] {NVDA AI accelerator};
  \fill (5.55,2.40) circle (2pt) node[right, font=\tiny] {MSFT cloud (AI demand)};
  % query
  \node[star, star points=5, star point ratio=2.3, fill=black, inner sep=1.3pt] at (4.95,3.15) {};
  \node[above, font=\tiny] at (4.95,3.28) {query};
  % neighbourhood
  \draw[dashed, black!55] (5.25,2.8) ellipse (1.05 and 0.95);
  \node[font=\tiny, text=black!55] at (5.25,1.65) {top-$k$ neighbourhood};
  % unrelated (hollow)
  \draw (1.15,0.7) circle (2pt) node[right, font=\tiny] {TSLA recall};
  \draw (0.95,2.5) circle (2pt) node[right, font=\tiny] {XOM oil-price move};
  \draw (1.55,3.4) circle (2pt) node[right, font=\tiny] {JPM rate outlook};
\end{tikzpicture}
\caption[Conceptual illustration of semantic retrieval]{Conceptual illustration of semantic retrieval.
Each headline becomes a point in a high-dimensional embedding space (drawn in two dimensions for intuition
only). Texts on the same theme lie close together, so a query (star) falls near its semantically similar
precedents (filled), which form the top-$k$ neighbourhood, and far from unrelated news (hollow). The next
table makes this concrete with real numbers.}
\label{fig:embedding_concept}
\end{figure}

Table~\ref{tab:retrieval_worked} then carries the step through end to end on the small sample knowledge
base bundled with the system. To keep the walk-through fully reproducible without downloading any model, it
uses a transparent baseline encoder whose similarity reflects word overlap directly; the deployed system
uses the Sentence-BERT encoder described above, whose retrieval quality is evaluated in
Chapter~\ref{chap:casestudies}. For the query \emph{``Nvidia demand surges on AI chip orders''}, the three
nearest precedents are all AI-themed and --- importantly --- not all of the same company: a Microsoft
cloud-AI item is retrieved alongside the two Nvidia items, illustrating the \emph{cross-ticker}
generalisation on which the evaluation rests. Reusing their observed outcomes gives a mean $+5$-day impact
of $+6.5\%$, which the alert presents as evidence, with the explicit caveat that it records what followed
comparable past news rather than a forecast.

\begin{table}[ht]
\caption{Worked retrieval example on the bundled sample knowledge base. Query:
\emph{``Nvidia demand surges on AI chip orders''}; similarities and impacts are real and reproducible.}
\label{tab:retrieval_worked}
\centering
\begin{tabular}{c c c p{0.44\textwidth}}
\toprule
\tabhead{Similarity} & \tabhead{+5-day impact} & \tabhead{Ticker / date} & \tabhead{Retrieved headline (excerpt)} \\
\midrule
$0.60$ & $+3.5\%$  & NVDA, 2023-05-25 & Nvidia guidance surges on soaring demand for AI data-centre chips \\
$0.38$ & $+10.9\%$ & MSFT, 2023-04-25 & Microsoft cloud growth accelerates on AI demand \\
$0.38$ & $+4.9\%$  & NVDA, 2023-06-13 & Nvidia unveils new AI accelerator to extend data-centre lead \\
\midrule
\multicolumn{4}{l}{\textbf{Mean $+5$-day impact across the retrieved precedents: $+6.5\%$}} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Explanation Techniques}
\label{sec:met_xai}

Explanation is pursued primarily through transparent design, following the interpretable-by-design
argument reviewed in Section~\ref{sec:sota_xai} \autocite{rudin2019stop, arrieta2020xai}. For every alert
the system assembles three transparent ingredients: the rule or measure that produced the detection (the
$z$-score together with its window and threshold); the retrieved historical precedents and their observed
impact, shown as direct evidence; and, optionally, feature attribution over the detector using SHAP
\autocite{lundberg2017shap}. Financial sentiment from a domain language model \autocite{araci2019finbert}
is an optional enrichment that is included only if it adds defensible value. Because each alert is
rendered directly from these computed objects, the explanation is faithful to the system's actual logic by
construction --- a property assessed explicitly in Chapter~\ref{chap:casestudies}.

\section{Tools and Frameworks}
\label{sec:met_tools}

The system is built on the open-source scientific-Python ecosystem, using mature, well-documented and
freely available libraries for numerical computing, market-data access, sentence embeddings and figure
generation. This choice is consistent with the project's constraint of using only free resources and with
its emphasis on reproducibility: pinned dependencies and fixed random seeds mean the environment and the
results can be recreated on another machine. The concrete engineering --- the environment, the
organisation of the code by component, and the automated tests --- is documented at the appropriate level
in Appendix~\ref{app:reproducibility}, deliberately kept out of the main text so that this chapter
remains a description of \emph{methods} rather than of software.

\section{Responsible AI and Ethics}
\label{sec:met_ethics}

The work is bound by several responsible-AI commitments that follow directly from its purpose and its
audience. \textbf{No advice and no prediction:} the system performs neither price prediction nor trading
recommendation; it surfaces observed past outcomes as evidence and states this explicitly in every alert,
so that the investor is informed rather than instructed. \textbf{Transparency:} every alert exposes its
full reasoning chain, in keeping with the explainability principles of Chapter~\ref{chap:sota}, so that a
user can verify rather than merely trust the output. \textbf{Honest evidence:} only results that can be
reproduced and explained are reported, and no data, metric or citation is fabricated. \textbf{Data
stewardship:} all sources are used within their free tiers and licences, the FNSPID dataset is attributed
as its Creative Commons licence requires \autocite{dong2024fnspid}, third-party news text is not
republished, and no personal data is processed. \textbf{Security:} credentials are never stored in
versioned files. Finally, the system is positioned as a decision-support aid for a non-professional
investor, not as a substitute for professional advice --- a limitation stated plainly rather than
obscured.

\section{Evaluation Methodology}
\label{sec:met_evaluation}

The evaluation is defined in advance, before any experiment is run. Fixing the metrics, baselines and
protocol beforehand is a deliberate guard against the temptation to choose, after the fact, the measure
that flatters the result --- a form of pre-registration in miniature. The full results are reported in
Chapter~\ref{chap:casestudies}; what follows is the protocol they obey.

\subsection{Retrieval metric and relevance}
\label{sec:met_eval_retrieval}

The correlation engine is evaluated as an information-retrieval task (Section~\ref{sec:sota_ir}), using
precision@$k$ --- the share of the top $k$ retrieved precedents that are relevant. Writing $Q$ for the set
of query headlines and $R_k(q)$ for the top $k$ items returned for query $q$,
\begin{equation}
\text{precision@}k = \frac{1}{|Q|}\sum_{q\in Q}\frac{1}{k}\sum_{d\in R_k(q)} \mathrm{rel}(q,d),
\qquad
\mathrm{rel}(q,d)=\mathbf{1}\big[\,\mathrm{sector}(d)=\mathrm{sector}(q)\,\big].
\label{eq:precatk}
\end{equation}
Relevance is approximated by \emph{sector agreement}: a precedent is counted as relevant when its company
belongs to the same economic sector as the query's. This is an automatic proxy for the human judgement of
``analogous'', adopted because no labelled relevance set exists; its limits are stated openly with the
results. Two design choices make the metric demanding rather than flattering. First, a strict
\emph{cross-ticker} constraint removes from the candidate pool every precedent that shares the query's
ticker, so the system cannot score by trivially matching a company to its own past news --- it must
generalise \emph{across} companies within a sector. Second, $k$ is kept small (the headline figure uses
$k=5$), reflecting that an alert can show only a handful of precedents, so the test is whether the very
top results are relevant.

\subsection{Baselines}
\label{sec:met_eval_baselines}

A single precision number means little in isolation, so semantic retrieval is compared against three
baselines, each chosen to control for a specific rival explanation. The \textbf{random} baseline draws $k$
precedents uniformly at random; its expected precision is just the base rate of same-sector items, and it
therefore marks the ``no-skill'' floor. The \textbf{recency} baseline returns the $k$ most recent items,
testing whether simply surfacing the newest news --- a tempting and cheap strategy --- would do as well.
The \textbf{lexical} baseline ranks by a bag-of-words, term-overlap representation under the same cosine
ranking, so that the difference between it and the embedding model isolates the marginal value of
\emph{semantic} meaning over surface word matching --- the central claim of the engine. Because each query
sample is drawn at random, the whole comparison is repeated over five random seeds and reported as a mean
with its standard deviation, so that a single lucky or unlucky draw cannot drive the conclusion; the
embedding model itself is varied between two Sentence-BERT variants to show the result is a property of
semantic retrieval, not of one specific model.

\subsection{Anomaly-detection protocol}
\label{sec:met_eval_anomaly}

The anomaly detector is judged primarily by an argument that needs \emph{no label}: a good universal
detector should fire at a comparable rate across instruments of very different volatility, whereas a
volatility-blind rule cannot. Consistency is therefore measured as the range (maximum minus minimum) of
per-ticker firing rates --- the smaller the range, the more uniform the detector. Only secondarily is the
detector scored against an explicit \emph{extreme-move} proxy (a daily move at or beyond the $99$th
percentile of that ticker's own returns), via precision, recall and $F_1$; because this proxy is itself
volatility-relative, it is treated as supporting rather than primary evidence. An ablation over the
estimation-window length completes the picture by exposing the detector's sensitivity to its one main
parameter.

\subsection{Explanation, scope, and rigour guarantees}
\label{sec:met_eval_rigour}

The explanation engine is assessed for \emph{fidelity} --- whether the alert text reflects the system's
actual computation, which holds by construction because each alert is rendered directly from the computed
objects --- while its \emph{usefulness} to a real investor is acknowledged as requiring a human study not
yet conducted, and is reported as a limitation rather than a claim. Because the system performs neither
price prediction nor trading, profit-based metrics are out of scope by design, not omitted by convenience.
Underpinning every experiment are three guarantees. First, \textbf{no lookahead}: any feature computed at
an instant uses only information available at or before that instant, and historical impact uses only
post-event windows. Second, \textbf{reproducibility}: random seeds are fixed, dependencies are pinned, and
every data and results figure is produced by a versioned script. Third, \textbf{honesty}: results are
reported with their caveats, and modest but genuine findings are preferred to inflated ones.

\section{Chapter Conclusions}
\label{sec:met_summary}

This chapter set out the methodology of defensible simplicity and incremental delivery, documented the
two-layer data architecture in a reproducible data card, justified the models and techniques chosen for
detection, retrieval, impact measurement and explanation, stated the responsible-AI commitments, and
fixed the evaluation methodology and rigour guarantees in advance. The next chapter shows how these
methods are integrated into CLARION, the proposed system.
