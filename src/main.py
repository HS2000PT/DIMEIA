"""Ponto de entrada do sistema (stub).

Nesta fase (Sessão 0 — Setup) é apenas um marcador. A orquestração real
do pipeline (gatilhos -> deteção/correlação -> explicação -> alerta Telegram)
será implementada a partir da "thin slice" end-to-end (~Sessão 10).
"""


def main() -> None:
    """Arranque do sistema. Ainda não implementado."""
    raise NotImplementedError(
        "Pipeline ainda não implementado — ver progress/PLANO_SESSOES.md."
    )


if __name__ == "__main__":
    main()
