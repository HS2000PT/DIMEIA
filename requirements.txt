# Dependências BASE (stack leve, Python 3.12). Instala-se com:  bash scripts/setup_env.sh
#
# Esta lista chega para TUDO o que um examinador precisa de correr sem descarregar modelos:
#   - a demo (scripts/demo.py: gatilho de notícia offline + gatilho de mercado ao vivo),
#   - os testes (bash scripts/verify.sh — exclui @sbert e @telegram),
#   - as avaliações sobre dados já guardados e as figuras.
#
# A stack pesada de ML (torch/SBERT) está em requirements-ml.txt e SÓ é precisa para a
# recuperação semântica real (SbertEmbedder) e os testes @sbert. Instala-se com:
#   bash scripts/setup_env.sh --ml      (usa o índice CPU da PyTorch — ver requirements-ml.txt)

# ── Ferramentas de desenvolvimento ──────────────────────────
pytest==8.3.4
ruff==0.8.6

# ── Núcleo / utilitários ────────────────────────────────────
python-dotenv==1.0.1
pandas==2.2.3
numpy==2.1.3
requests==2.32.3
PyYAML==6.0.2

# ── Figuras reprodutíveis ───────────────────────────────────
matplotlib==3.11.0

# ── Camada live (mercado) ───────────────────────────────────
yfinance==1.4.1

# ── Triagem de materialidade (modelos treinados; CPU, leve) ─
# O treino com embeddings SBERT precisa da stack pesada (--ml); mas treinar/inferir com o
# bloco de contexto (e correr os testes) funciona só com isto.
scikit-learn==1.9.0
joblib==1.5.3

# ── Retrieval SEMÂNTICO leve (produto: app pública + runner) ─
# O MESMO MiniLM da tese, exportado em ONNX (~23 MB descarregados sob demanda com SHA256
# pinado — o modelo NÃO é versionado). Sem torch: onnxruntime CPU + tokenizers (Rust).
# Se faltar (sem rede/sem cache), o produto degrada para word-overlap — nada parte.
onnxruntime==1.27.0
tokenizers==0.22.2

# ── A app (dashboard) ───────────────────────────────────────
# O plotly desenha o gráfico interativo; o streamlit serve a app.
# Ambos são puro Python (sem binários pesados) e instalam em qualquer versão.
#
# Porque é que o streamlit está AQUI e não só no requirements-app.txt: o Heroku lê
# apenas este ficheiro. No Streamlit Community Cloud a plataforma já o traz
# pré-instalado, por isso lá é redundante — mas inofensivo, e ter a app a arrancar
# depois de um único `pip install -r requirements.txt` é melhor do que a alternativa.
plotly==6.8.0
streamlit==1.41.1

# O próprio pacote (editável): torna `investigator` importável sem hacks de sys.path.
-e .
