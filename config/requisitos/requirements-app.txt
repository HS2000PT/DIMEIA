# ⚠️ SUPERADO (2026-08-01): o `streamlit` passou para o requirements.txt.
#
# Porquê: o Heroku lê APENAS o requirements.txt, e lá a app é um processo de produção.
# Manter a dependência num ficheiro à parte fazia o deploy arrancar sem o streamlit
# instalado. O plotly (também usado só pela app) já vivia no requirements.txt, por isso
# esta é a arrumação coerente e não uma exceção.
#
# Este ficheiro fica por compatibilidade — instalá-lo continua a funcionar, e há
# documentação que lhe chama. Para correr a app localmente basta agora:
#
#   pip install -r requirements.txt
#   streamlit run app/streamlit_app.py

streamlit==1.41.1
